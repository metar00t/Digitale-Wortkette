Join an available Lobby
---
tags:
  - Player
parameters:
  - in: body
    name: User_Details
    required:
      - username
    properties:
      username:
        type: string
        description: Name of the User
      status:
        type: boolean
        description: Status of Player, if ready to start the Game
responses:
  201:
    description: Player created
  204:
    description: Playerlist not found