Join Lobby
---
tags:
  - Player
parameters:
  - in: body
    name: user
    required:
      - nickname
    properties:
      nickname:
        type: string
        description: Name of the User
      isPlayerReady:
        type: boolean
        description: Status of Player, set to False by Default.
  - in: path
    name: lobbyID
    required: True
    description: The LobbyID to which a Player wants to join
    schema:
      type: Integer
responses:
  201:
    description: Player created