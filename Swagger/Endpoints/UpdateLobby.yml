Update the Lobby with the chosen Settings in the App
---
tags:
  - Host
parameters:
  - in: body
    name: Lobby
    required:
      - subjectName
      - maxPlayers
      - maxGameLength
    properties:
      subject:
        type: string
        description: Topic of the Game Session
      maxPlayers:
        type: integer
        description: Sets the maximum Amount of Players that can join the Lobby
      maxGameLength:
        type: integer
        description: Sets the maximum Length of the Game
responses:
  200:
    description: Lobby updated